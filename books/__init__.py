"""Books — a standalone site feature (book ranking + community suggestions).

Not a game and not part of Spender: the React page lives in Books.jsx and the
FastAPI routes/DB logic in api.py (attached to the shared app via setup_books).
"""
