# H3 sandbox run artifacts (scratch eval outputs / logs / autotune results) -- not source
h3_*.out
*.console.log
h3_autotune.log
h3_best.json
# variant-S (vsearch) tuning scratch -- outputs/logs only; the .py harnesses are source
vsearch_*.out
vsearch_autotune.log
vsearch_best.json
v_state_eval*.out
*cache*.npz
