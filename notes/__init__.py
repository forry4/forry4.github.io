"""Notes — a private, owner-only notebook (a site feature, not a game).

Nested folders of notes; each note is a flowing rich-text document (TipTap JSON)
with inline screenshots. The React page lives in Notes.jsx and the FastAPI
routes/DB logic in api.py (attached to the shared app via setup_notes).
"""
