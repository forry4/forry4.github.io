# Spender webapp

Minimal Vite + React wrapper around the existing `Spender.jsx` component for local development.

Run:

```powershell
cd games/spender/webapp
npm install
npm run dev
```

Open http://localhost:5173 and the app will mount the existing component.
