# PINNED (2026-09-22). This file is the SERVER's: the Dockerfile builds the prod
# image from it, and python-app.yml / deploy-render.yml test against it on 3.14.
# Unpinned, every deploy silently took whatever was newest on PyPI that day, so a
# third-party release could change prod on a push that touched none of this. The
# pins are what 3.11 resolved at the time, i.e. what prod and the last green CI
# were already running; every one has a cp314 (or pure-Python) wheel, which is
# what made the 3.11 -> 3.14 move a version bump rather than a dependency bump.
# Bump deliberately; CI gates the bump before it deploys.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
websockets==17.1
pytest==9.1.1
pytest-xdist==3.8.0 # the suite runs `-n auto` by default (pytest.ini addopts) — NOT optional
numpy==2.5.3
libsql==0.1.11      # Turso/libSQL client for persistent prod DB (imported only when TURSO_DATABASE_URL is set)
