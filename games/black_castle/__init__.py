"""The Black Castle — Himeji Castle worker-placement game.

The package deliberately keeps the rules in :mod:`engine` so the websocket,
the Easy bot, replay tooling, and future BGA validation all share one source of
truth.
"""

